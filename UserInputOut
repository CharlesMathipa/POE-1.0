public class UserInputOut {
    public static void main(String[] args) {
        try (Scanner scanner = new Scanner(System.in)) {
            Login login = new Login();
            boolean exitProgram = false;

            //I initialized these variables to store data across menu choices
            String name;
            String surname;
            String username = "";
            String password = "";
            String phoneNumber = "";
            boolean checkUserName;
            boolean checkPasswordComplexity;
            boolean checkCellPhoneNumber;

            
            int maxTasks = 100; 
            String[] developers = new String[maxTasks];
            String[] taskNames = new String[maxTasks];
            String[] taskIDs = new String[maxTasks];
            int[] taskDurations = new int[maxTasks];
            String[] taskStatuses = new String[maxTasks];
            int taskCount = 0; 

            while (!exitProgram) {
                System.out.println("\n=== NEOAPP MENU ===");
                System.out.println("1. Register");
                System.out.println("2. Login");
                System.out.println("3. Exit");
                System.out.print("Please choose an option (1-3): ");

                String choice = scanner.nextLine();

                switch (choice) {
                    case "1" -> {
                        System.out.println("\n--- REGISTRATION ---");
                        System.out.print("Enter your Name: ");
                        name = scanner.nextLine();
                        System.out.print("Enter your Surname: ");
                        surname = scanner.nextLine();

                        checkUserName = false;
                        while (!checkUserName) {
                            System.out.print("Enter a username: ");
                            username = scanner.nextLine();
                            if (login.checkUserName(username)) {
                                System.out.println("Username successfully captured.");
                                checkUserName = true;
                            } else {
                                System.out.println("Username is not correctly formatted; please ensure that your username contains an underscore and is no more than five characters in length.");
                            }
                        }

                        checkPasswordComplexity = false;
                        while (!checkPasswordComplexity) {
                            System.out.print("Enter a password: ");
                            password = scanner.nextLine();
                            if (login.checkPasswordComplexity(password)) {
                                System.out.println("Password successfully captured.");
                                checkPasswordComplexity = true;
                            } else {
                                System.out.println("Password is not correctly formatted; please ensure that your password contains at least eight characters, a capital letter, a number, and a special character.");
                            }
                        }

                        checkCellPhoneNumber = false;
                        while (!checkCellPhoneNumber) {
                            System.out.print("Enter your phone number with the international South African code included (e.g., +27123456789): ");
                            phoneNumber = scanner.nextLine();
                            if (login.checkPhoneNumber(phoneNumber)) {
                                System.out.println("Phone number successfully added.");
                                checkCellPhoneNumber = true;
                            } else {
                                System.out.println("Cell phone number is incorrectly formatted or does not contain the international code.");
                            }
                        }

                        System.out.println(login.registerUser(username, password, name, surname, phoneNumber));
                    }

                    case "2" -> {
                        System.out.println("\n--- LOGIN ---");

                        if (username.isEmpty() || password.isEmpty()) {
                            System.out.println("No registered user found. Please register first.");
                            continue; 
                        }
                        
                        String loginUserName = "";
                        checkUserName = false;
                        while (!checkUserName) {
                            System.out.print("Enter your username to login: ");
                            loginUserName = scanner.nextLine();
                            if (login.checkUserName(loginUserName)) {
                                System.out.println("Username successfully captured."); 
                                checkUserName = true;
                            } else {
                                System.out.println("Username is not correctly formatted; please ensure that the username contains an underscore and is no more than five characters long.");
                            }
                        }
                        
                        String loginPassword = "";
                        checkPasswordComplexity = false;
                        while (!checkPasswordComplexity) {
                            System.out.print("Enter your password to login: ");
                            loginPassword = scanner.nextLine();
                            if (login.checkPasswordComplexity(loginPassword)) {
                                System.out.println("Password successfully captured.");
                                checkPasswordComplexity = true;
                            } else {
                                System.out.println("Password is not correctly formatted; please ensure that the password contains at least eight characters, a capital letter, a number, and a special character.");
                            }
                        }
                        
                        if (isValidLogin) {
                            System.out.println("A successful login.");
                            System.out.println("\nWelcome to EasyKanban");
                            
                            boolean exitKanban = false;
                            int totalHours = 0; 

                            
                            while (!exitKanban) {
                                System.out.println("\n=== EASYKANBAN MENU ===");
                                System.out.println("1. Add tasks");
                                System.out.println("2. Show report");
                                System.out.println("3. Quit");
                                System.out.print("Please choose an option (1-3): ");

                                String kanbanChoice = scanner.nextLine();

                                switch (kanbanChoice) {
                                    case "1" -> {
                                        System.out.print("How many tasks would you like to enter? ");
                                        
                                        int numTasks = Integer.parseInt(scanner.nextLine()); 

                                        for (int i = 0; i < numTasks; i++) {
                                            System.out.println("\n--- ENTERING TASK " + (i + 1) + " ---");

                                            System.out.print("Enter Task Name: ");
                                            String taskName = scanner.nextLine();

                                            String taskDescription = "";
                                            boolean validDescription = false;
                                            
                                            while (!validDescription) {
                                                System.out.print("Enter Task Description (max 50 chars): ");
                                                taskDescription = scanner.nextLine();
                                                
                                                Task tempTask = new Task("", 0, taskDescription, "", 0, "");
                                                if (tempTask.checkTaskDescription()) {
                                                    System.out.println("Task description successfully captured.");
                                                    validDescription = true;
                                                } else {
                                                    System.out.println("Please enter a task description of less than 50 characters.");
                                                }
                                            }

                                            System.out.print("Enter Developer First and Last Name: ");
                                            String devDetails = scanner.nextLine();

                                            System.out.print("Enter Task Duration (in hours): ");
                                            int taskDuration = Integer.parseInt(scanner.nextLine());

                                            String taskStatus = "";
                                            boolean validStatus = false;
                                            
                                            while (!validStatus) {
                                                System.out.println("Select Task Status:");
                                                System.out.println("1. To Do");
                                                System.out.println("2. Doing");
                                                System.out.println("3. Done");
                                                System.out.print("Choice (1-3): ");
                                                String statusChoice = scanner.nextLine();
                                                
                                                if (statusChoice.equals("1")) { taskStatus = "To Do"; validStatus = true; }
                                                else if (statusChoice.equals("2")) { taskStatus = "Doing"; validStatus = true; }
                                                else if (statusChoice.equals("3")) { taskStatus = "Done"; validStatus = true; }
                                                else { System.out.println("Invalid choice. Please select 1, 2, or 3."); }
                                            }

                                            Task newTask = new Task(taskName, i, taskDescription, devDetails, taskDuration, taskStatus);

                                            
                                            System.out.println("\n--- TASK CAPTURED SUCCESSFULLY ---");
                                            System.out.println(newTask.printTaskDetails());

                                            
                                            totalHours += newTask.returnTotalHours();

                                            
                                            totalHours += newTask.returnTotalHours();

                                            
                                            developers[taskCount] = devDetails;
                                            taskNames[taskCount] = taskName;
                                            taskIDs[taskCount] = newTask.createTaskID();
                                            taskDurations[taskCount] = taskDuration;
                                            taskStatuses[taskCount] = taskStatus;
                                            
                                            taskCount++;
                                        
                                        
                                        }
                                        
                                        
                                        System.out.println("\nTotal hours across all entered tasks: " + totalHours + " hrs");
                                    }
                                    
                                    case "2" -> {
                                        boolean exitReport = false;
                                        while (!exitReport) {
                                            System.out.println("\n--- TASK REPORT MENU ---");
                                            System.out.println("1. Show completed tasks (Done)");
                                            System.out.println("2. Show task with longest duration");
                                            System.out.println("3. Search for a task by name");
                                            System.out.println("4. Search tasks by developer");
                                            System.out.println("5. Delete a task");
                                            System.out.println("6. Show full task report");
                                            System.out.println("7. Return to EasyKanban Menu");
                                            System.out.print("Please choose an option (1-7): ");

                                            String reportChoice = scanner.nextLine();

                                            switch (reportChoice) {
                                                case "1" -> {
                                                    System.out.println("\n--- COMPLETED TASKS ---");
                                                    boolean found = false;
                                                    for (int i = 0; i < taskCount; i++) {
                                                        
                                                        if (taskStatuses[i] != null && taskStatuses[i].equalsIgnoreCase("Done")) {
                                                            System.out.println("Developer: " + developers[i] + " | Task Name: " + taskNames[i] + " | Duration: " + taskDurations[i] + "hrs");
                                                            found = true;
                                                        }
                                                    }
                                                    if (!found) System.out.println("No completed tasks found.");
                                                }
                                                case "2" -> {
                                                    System.out.println("\n--- LONGEST TASK ---");
                                                    if (taskCount == 0) {
                                                        System.out.println("No tasks have been entered yet.");
                                                    } else {
                                                        int maxIndex = 0;
                                                        for (int i = 1; i < taskCount; i++) {
                                                           
                                                            if (taskDurations[i] > taskDurations[maxIndex]) {
                                                                maxIndex = i;
                                                            }
                                                        }
                                                        System.out.println("Developer: " + developers[maxIndex] + " | Duration: " + taskDurations[maxIndex] + "hrs");
                                                    }
                                                }
                                                case "3" -> {
                                                    System.out.print("\nEnter the exact Task Name to search: ");
                                                    String searchName = scanner.nextLine();
                                                    boolean found = false;
                                                    for (int i = 0; i < taskCount; i++) {
                                                        if (taskNames[i] != null && taskNames[i].equalsIgnoreCase(searchName)) {
                                                            System.out.println("Task Name: " + taskNames[i] + " | Developer: " + developers[i] + " | Status: " + taskStatuses[i]);
                                                            found = true;
                                                        }
                                                    }
                                                    if (!found) System.out.println("Task not found.");
                                                }
                                                case "4" -> {
                                                    System.out.print("\nEnter the Developer Name to search: ");
                                                    String searchDev = scanner.nextLine();
                                                    boolean found = false;
                                                    for (int i = 0; i < taskCount; i++) {
                                                        if (developers[i] != null && developers[i].equalsIgnoreCase(searchDev)) {
                                                            System.out.println("Task Name: " + taskNames[i] + " | Status: " + taskStatuses[i]);
                                                            found = true;
                                                        }
                                                    }
                                                    if (!found) System.out.println("No tasks found for this developer.");
                                                }
                                                case "5" -> {
                                                    System.out.print("\nEnter the exact Task Name to delete: ");
                                                    String deleteName = scanner.nextLine();
                                                    boolean deleted = false;
                                                    for (int i = 0; i < taskCount; i++) {
                                                        if (taskNames[i] != null && taskNames[i].equalsIgnoreCase(deleteName)) {
                                                           
                                                            taskNames[i] = "DELETED";
                                                            System.out.println("Task '" + deleteName + "' successfully deleted.");
                                                            deleted = true;
                                                            break; 
                                                        }
                                                    }
                                                    if (!deleted) System.out.println("Task not found.");
                                                }
                                                case "6" -> {
                                                    System.out.println("\n--- FULL TASK REPORT ---");
                                                    if (taskCount == 0) System.out.println("No tasks available.");
                                                    for (int i = 0; i < taskCount; i++) {
                                                        // Only print tasks that haven't been deleted
                                                        if (taskNames[i] != null && !taskNames[i].equals("DELETED")) {
                                                            System.out.println("Task ID: " + taskIDs[i] + " | Name: " + taskNames[i] + " | Dev: " + developers[i] + " | Duration: " + taskDurations[i] + "hrs | Status: " + taskStatuses[i]);
                                                        }
                                                    }
                                                }
                                                case "7" -> {
                                                    System.out.println("Returning to EasyKanban Menu...");
                                                    exitReport = true; 
                                                default -> System.out.println("Invalid option. Please choose 1-7.");
                                            }
                                        }
                                    }
                                    
                                    case "3" -> {
                                        System.out.println("\nExiting EasyKanban... Returning to Main Menu.");
                                        exitKanban = true; 
                                    }
                                    
                                    default -> System.out.println("\nInvalid selection. Please choose 1, 2, or 3.");
                                }
                            }

                        } else {
                            System.out.println("Login failed. Please check your credentials.");
                        }
                    }

                    case "3" -> {
                        System.out.println("Exiting NEOAPP... Goodbye!");
                        exitProgram = true;
                    }

                    default -> System.out.println("Invalid selection. Please choose 1, 2, or 3.");
                }
            }
        }
    }
}